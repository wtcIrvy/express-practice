<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title>Home Page</title>
    </head>
    <body>
        {{> header}}
        <div class="container-fluid">
            <div class="row justify-content-center">
                <div class="col-10">
                    <div class="row justify-content-center">
                        <h1>{{pageTitle}}</h1>
                    </div>
                    <div class="row justify-content-center">
                        <p>{{shoutIt welcomeMessage}}</p>
                        
                    </div>
                    <div class="row justify-content-center">
                        <p>{{shoutIt additionalInfo}}</p>
                    </div>
                </div>
            </div>
        </div>
        {{> footer}}
    </body>
</html>